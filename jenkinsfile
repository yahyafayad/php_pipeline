pipeline {
    agent any

    environment {
    }

    stages {
        stage('Git checkout') {
            steps {
                git branch: 'main', credentialsId: 'git_crad', url: 'https://github.com/yahyafayad/php_pipeline.git'
            }
        }

        
        
    }
}

